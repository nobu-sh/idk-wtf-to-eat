{
  "name": "root",
  "private": true,
  "scripts": {
    "install:client": "cd client && yarn",
    "install:server": "cd server && yarn",
    "build:client": "cd client && yarn build",
    "build:server": "cd server && yarn build",
    "start": "cd server && yarn start",
    "deploy": "cd server && yarn deploy",
    "green-means-go": "yarn install:client && yarn install:server && yarn build:client && yarn build:server && yarn start",
    "green-means-deploy": "yarn install:client && yarn install:server && yarn build:client && yarn build:server && yarn deploy"
  }
}
